// didn't think i'd have to filter names

pub fn filter(name: String) -> String {
    name.replace("nigga", "MEOW")
        .replace("nigger", "MEOW")
        .replace("tranny", "MEOW")
        .replace("faggot", "MEOW")
}
